server {
    listen 80;
    server_name aigen.d-auchy.studio;

    root /var/www/aigen.d-auchy.studio;
    index index.html;

    access_log /var/log/nginx/aigen-access.log;
    error_log /var/log/nginx/aigen-error.log;

    location / {
        try_files $uri $uri/ /index.html;
    }
}

